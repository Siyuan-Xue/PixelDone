PixelDone v2.0.1 fixes system bar contrast on the light PixelDone surface.

Highlights:
- Explicitly requests dark system bar icons for PixelDone's light app surface.
- Keeps edge-to-edge layout behavior aligned with the existing PixelPark app chrome.
- Leaves the todo, checklist, alarm, and update workflows unchanged.

Install note: if you still have the old `com.codexue.pixeldone` prototype build, uninstall it before installing this private release.

The attached `PixelDone-2.0.1-release.apk` asset is the signed private release APK for direct installation and app-internal updates.

CODEX & XUE
